---
category: Product
---

# PipelineProgress

Live progress for the verification pipeline — a list of steps, each `pending` / `active` / `done` / `error` with an optional message. Known step ids (`extract` / `search` / `verify` / `report`) get a lucide icon.
