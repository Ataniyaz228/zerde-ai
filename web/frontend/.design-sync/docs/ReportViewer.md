---
category: Product
---

# ReportViewer

Markdown report renderer. Highlights an in-text search query, swaps verdict status emojis for lucide icons, turns status tags into coloured pills and callouts, wraps wide tables in a horizontal scroller, and exposes copy-link / download-`.md` actions. Pass the report body as `content`.
