---
category: Verdict
---

# VerdictBadges

Verdict summary for a report. `variant="full"` shows an answer headline + a confirmed/contradicted/unverified ratio bar + legend (report header); `variant="compact"` shows inline status pills (table rows). Counts carry `confirmed` / `contradicted` / `unverified` / `total` / `coverage_pct`.
