---
category: Product
---

# AnimatedNumber

Counts up from 0 to `to` once, when it first scrolls into view. Uses a thin-space thousands separator (editorial number style) and respects `prefers-reduced-motion` (jumps straight to the final value). Optional `suffix` (e.g. `"+"`) and `duration` (ms).
