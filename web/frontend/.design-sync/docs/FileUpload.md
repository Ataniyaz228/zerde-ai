---
category: Product
---

# FileUpload

Drag-and-drop / picker for a draft-law document (`.pdf` / `.doc` / `.docx` / `.txt`, ≤ 25 MB). Validates extension and size, then calls `onAnalyze(file)`. Shows a loading state while `isLoading` is true.
