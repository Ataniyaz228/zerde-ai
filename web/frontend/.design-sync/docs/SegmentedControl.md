---
category: Controls
---

# SegmentedControl

Segmented toggle — a row of `aria-pressed` buttons sharing one track. Used for report filters / sort and the RU/KZ language switch. `size="md"` is the default; `size="sm"` is compact. Controlled via `value` + `onChange`.
