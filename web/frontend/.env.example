NEXT_PUBLIC_API_URL=http://localhost:8000
# Задавай, только если бэкенд запущен с ZERDE_API_KEY. Значение уходит в браузер
# (NEXT_PUBLIC_*), поэтому это не секрет, а заслонка от случайных запросов;
# реальную защиту даёт rate-limit и (позже) пользовательская авторизация.
NEXT_PUBLIC_API_KEY=
